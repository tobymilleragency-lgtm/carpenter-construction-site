
const toggle=document.querySelector('#mobileToggle');const links=document.querySelector('#navLinks');if(toggle){toggle.addEventListener('click',()=>{links.classList.toggle('open');toggle.setAttribute('aria-expanded',links.classList.contains('open'))})}
const form=document.querySelector('#scopeForm');const out=document.querySelector('#scopeSummary');function selectedScopes(){return [...document.querySelectorAll('.scope-choice.selected')].map(x=>x.dataset.scope)}function buildSummary(){if(!form||!out)return;const data=new FormData(form);const types=selectedScopes();out.textContent=`Carpenter Construction estimate-prep summary\n\nName: ${data.get('name')||'—'}\nPhone: ${data.get('phone')||'—'}\nCity: ${data.get('city')||'—'}\nProject types: ${types.join(', ')||'—'}\nNotes: ${data.get('notes')||'—'}\n\nNext step: call (417) 850-4491. This form prepares your scope summary locally; it does not send until delivery is wired.`}document.querySelectorAll('.scope-choice').forEach(btn=>btn.addEventListener('click',()=>{btn.classList.toggle('selected');buildSummary()}));if(form){form.addEventListener('input',buildSummary);buildSummary();form.addEventListener('submit',e=>{e.preventDefault();buildSummary();out.scrollIntoView({behavior:'smooth',block:'center'})})}
